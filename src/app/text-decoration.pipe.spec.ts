import { TextDecorationPipe } from './text-decoration.pipe';

describe('TextDecorationPipe', () => {
  it('create an instance', () => {
    const pipe = new TextDecorationPipe();
    expect(pipe).toBeTruthy();
  });
});
